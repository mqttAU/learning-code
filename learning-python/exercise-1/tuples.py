# tuples are used to store multiple items in a single variable. tuple is non mutable (non modifiable). used for data that is not going to change
# for example a typle is represented as a comma-delimited list of the elements, enclosed in parentheses.
# e.g. "(5, 9, 11, 3, 22, 14)" is a "6-tuple."
coordinates = (4, 5)
coordinates_2 = [(4, 5), (6, 7), (80, 34)]

print(coordinates[0])

print(coordinates_2[1])