# getting input from users, allowing users to input info into program and take info inputted and store it in var
# then do something with the var

name = input("Enter your name: ")
age = input("Enter your age: ")

print("Hello " + name + "! You are " + age + " years old.")