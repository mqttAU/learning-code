import useful_tools # importing the name of the file, python should know it will grab all the useful stuff, now u can use all the functions inside that file

print(useful_tools.roll_dice(10)) # u can access the attributes in that file that was imported .e.g. roll_dice

# importing functionality from external py files. notice that you didnt have to copy any functions variables etc.
#basics of using modules, pretty much just any external py file that u wanna use

# https://docs.python.org/3/py-modindex.html handy list of py modules, already written and u can access and import. already written code, if u want that functionality from one of these modules etc.

